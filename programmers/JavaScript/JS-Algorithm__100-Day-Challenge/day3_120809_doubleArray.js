// 코딩테스트 입문 > 배열 두 배 만들기

// 정수 배열 numbers가 매개변수로 주어집니다. numbers의 각 원소에 두배한 원소를 가진 배열을 return하도록 solution 함수를 완성해주세요.

function solution(numbers) {
    return doubleArray = numbers.map (double = num => num * 2) ;
    
}

solution([1, 2, 3, 4, 5])

// 앗 좀 더 하드코딩 식으로 쓰자면 이렇게 쓸 수 있네요.
function solution(numbers) {
    return doubleArray = numbers.map (num => num * 2) ;
    
}